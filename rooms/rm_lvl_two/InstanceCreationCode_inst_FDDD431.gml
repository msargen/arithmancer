// for now I'll set the value manually on both gem and socket
// ideally the socket will have a random equation generated and pass the answer to the correct gem

// the first one is hard coded 1+1=2
answer = 2;



//instance_destroy(inst_FDDD431);